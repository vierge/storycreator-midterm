-- INSERT INTO stories (owner_id, name, date_created, date_completed, is_locked, content_tags) VALUES
-- (x,x,x,x,x) RETURNING id;

-- const id = return

-- db.query(do the query).then(res => {
--   db.query do the next query(res)
--   .then(renderrrrr)
-- })
